# Build configuration.
# Publish credentials and signing keys are supplied at release time via env vars
# (OSSRH_USERNAME, OSSRH_PASSWORD, SIGNING_KEY, SIGNING_PASSWORD) or -P properties.
# They are intentionally NOT committed here.
org.gradle.caching=true
org.gradle.parallel=true
